export const initialValue = {
  userDetails: null,
};

export const reducer = (state, action) => {
  const { type, payload } = action || {};

  switch (type) {
    default:
      return state;
  }
};
